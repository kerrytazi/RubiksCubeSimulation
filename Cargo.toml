[package]
name = "RubiksCubeSimulation"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
three-d = { version = "0.19.0", features = ["text"] }

[profile.release]
codegen-units = 1
lto = "fat"
strip = true
panic = "abort"
